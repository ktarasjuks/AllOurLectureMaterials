package lecture3;

public class LightDetectorLauncher {
    public static void main(String[] args) {
        LightDetector mySuperlightDetector = new LightDetector();
        mySuperlightDetector.detect(400);
    }
}
